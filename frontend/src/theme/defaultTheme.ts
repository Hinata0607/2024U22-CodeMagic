'use client';
import { createTheme } from '@mui/material';

export const defaultTheme = createTheme({
	palette: {
		background: {
			default: '#fff',
		},
		layout: {
			topLayout: {
				main: '#000',
			},
		},
	},
});
